#ifndef XOR_SORT_HPP_
#define XOR_SORT_HPP_

#include <iostream>
#include <stdint.h>

using namespace std;

class XorSort {
    public:
        XorSort(void);
        ~XorSort(void);

    public:
        void mainFunction(void);
};

#endif //XOR_SORT_HPP_